#include "HumanStrategy.h"
#include "Player.h"
#include <vector>
using namespace std;

//report human havn't finished playing his turn
bool HumanStrategy::executeStrategy(Player& player, vector<Card>& table) {
	return false;
}